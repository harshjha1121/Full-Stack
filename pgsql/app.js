import React from "react";
import "./App.css";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <h1>🚀 React + Docker Multi-Stage Build</h1>
        <p>This app is running inside a Docker container using Nginx!</p>
      </header>
    </div>
  );
}

export default App;
